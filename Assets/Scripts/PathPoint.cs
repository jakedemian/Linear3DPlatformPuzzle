using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PathPoint : MonoBehaviour
{
    // these need to constantly poll the player and if 
    
    // mmmm maybe not, maybe each path needs a trigger plane where being between
    // the paths planes or something makes it the active one?
}
